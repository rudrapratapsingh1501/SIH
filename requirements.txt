fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
httpx==0.27.2
python-multipart==0.0.9
pytest==8.3.3
joblib==1.4.2
scikit-learn==1.5.2
numpy==1.26.4
librosa==0.10.2.post1
soundfile==0.12.1

# Optional heavy ML deps — the app runs fine without these (falls back to
# keyword rules + prosody heuristics), but install them for the full
# MuRIL + faster-whisper pipeline. Commented out by default because they
# significantly increase image size/build time and download models at
# runtime from Hugging Face.
# torch==2.4.1
# transformers==4.44.2
# faster-whisper==1.0.3
